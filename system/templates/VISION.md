# VISION

## Active Goal
No active goal. The next user coding prompt will be registered as a goal.

## Completion Conditions
- The active goal's deterministic checks pass.
- The independent verifier records evidence for every completion condition.
- The candidate diff stays inside the authorized project paths.
- The comprehensive validator exits 0.

## Out of Scope
- `.agentic/**` during ordinary project tasks.
- `AGENTS.md` during ordinary project tasks.
- Secrets, production credentials, and destructive production operations.

## Constraints
- Preserve existing behavior unless the user prompt explicitly changes it.
- Do not weaken tests or validators to obtain a pass.
- Do not invent subjective completion criteria at runtime.

## Type D Criteria — Pre-approved
None.
